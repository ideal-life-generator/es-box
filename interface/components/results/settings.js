import {
  infoWidth as infoWidthString,
  smallItemHeight as smallItemHeightString,
  normalItemHeight as normalItemHeightString,
} from './index.sass'

export const width = 1236

export const infoWidth = parseFloat(infoWidthString)

export const SMALL_ITEM_HEIGHT = parseFloat(smallItemHeightString)

export const NORMAL_ITEM_HEIGHT = parseFloat(normalItemHeightString)
