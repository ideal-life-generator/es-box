import {
  itemHeight as itemHeightString,
  infoWidth as infoWidthString,
} from './index.sass'

export const width = 1236

export const itemHeight = parseFloat(itemHeightString)
export const infoWidth = parseFloat(infoWidthString)
